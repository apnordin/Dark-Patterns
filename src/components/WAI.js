import React from "react";

export default function WAI ( { } ) {

    return (
        <div className="col-12 text-center">
            <h2 className="welcome-back">Wages and income
            </h2>
        </div>
    )
}